<Window x:Class="OnlineCinema.App.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InputDialog" Height="200" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" FontWeight="Bold" FontSize="15" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="1" Text="Название:"/>
        <TextBox x:Name="TbInput1" Grid.Row="2" Margin="0,5,0,10"/>
        <TextBlock Grid.Row="3" Text="Описание:"/>
        <TextBox x:Name="TbInput2" Grid.Row="4" Margin="0,5,0,10"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Click="Ok_Click" Width="80" Height="28" Background="#27ae60" Foreground="White"/>
            <Button Content="Отмена" Click="Cancel_Click" Width="80" Height="28" Margin="5,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>